# SHADOWGUARD Environment Configuration

# Ethereum Mainnet RPC (chain_id = 1)
# Using multiple public endpoints - first one that works is used
ETH_RPC_URL=https://ethereum-rpc.publicnode.com

# Fallback RPCs (comma-separated, tried in order)
ETH_RPC_FALLBACKS=https://rpc.ankr.com/eth,https://eth.drpc.org,https://cloudflare-eth.com


# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# SQLite database path
DB_PATH=shadowguard.db

# Policy configuration file path
POLICY_FILE=policy.json

# Simulation timeout in seconds
SIMULATION_TIMEOUT=15

# Gas anomaly threshold (percentage of gas limit)
GAS_ANOMALY_THRESHOLD=80

# Maximum nested call depth before flagging
MAX_NESTED_CALLS=5
